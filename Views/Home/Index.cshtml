<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width">
    <title>Party Invitation</title>
    <link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.css">
    <style>
        .image-container {
            margin-top:20px;
            text-align: center;
        }

        .image-container img {
            max-width: 35%;
            height: auto;
            border: 5px solid #3498db;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            transition: transform 0.9s;
        }

        .image-container img:hover {
            transform: translateY(10px);
        }

        .wrap {
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .btn.btn-primary {
            margin-top:50px;
            min-width: 300px;
            min-height: 60px;
            font-family: 'Nunito', sans-serif;
            font-size: 22px;
            text-transform: uppercase;
            letter-spacing: 1.3px;
            font-weight: 700;
            color: #313133;
            background: #4FD1C5;
            background: linear-gradient(90deg, rgba(129, 230, 217, 1) 0%, rgba(79, 209, 197, 1) 100%);
            border: none;
            border-radius: 1000px;
            box-shadow: 12px 12px 24px rgba(79, 209, 197, .64);
            transition: all 0.3s ease-in-out 0s;
            cursor: pointer;
            outline: none;
            position: relative;
            padding: 10px;
        }

        .btn.btn-primary::before {
            content: '';
            border-radius: 1000px;
            min-width: calc(300px + 12px);
            min-height: calc(60px + 12px);
            border: 6px solid #00FFCB;
            box-shadow: 0 0 60px rgba(0, 255, 203, .64);
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0;
            transition: all .3s ease-in-out 0s;
        }

        .btn.btn-primary:hover,
        .btn.btn-primary:focus {
            color: #313133;
            transform: translateY(-6px);
        }

        .btn.btn-primary:hover::before,
        .btn.btn-primary:focus::before {
            opacity: 1;
        }

        .btn.btn-primary::after {
            content: '';
            width: 30px;
            height: 30px;
            border-radius: 100%;
            border: 6px solid #00FFCB;
            position: absolute;
            z-index: -1;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation: ring 1.5s infinite;
        }

        .btn.btn-primary:hover::after,
        .btn.btn-primary:focus::after {
            animation: none;
            display: none;
        }

        
     
    </style>
</head>
<body>
    <div class="text-center m-2">
        <h3>We're going to have an exciting party!</h3>
        <h4>And YOU are invited!</h4>
        <div class="wrap">
            <a class="btn btn-primary" asp-action="RsvpForm">Click Here</a>
        </div>
        <div class="image-container">
            <img src="https://marketplace.canva.com/EAFLXHhoHj0/1/0/1600w/canva-rainbow-pastel-watercolour-birthday-invitation--q_KBQjxqRI.jpg" alt="Party Image" />
        </div>
    </div>
</body>
</html>
